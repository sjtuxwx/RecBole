eval_batch_size: 409600
train_batch_size: 40960
eval_args:
  mode: uni100
save_single_user: True          # (bool) Whether or not to save the single user interaction data.
save_base_path: output  # (str) Path to save the single user interaction data.